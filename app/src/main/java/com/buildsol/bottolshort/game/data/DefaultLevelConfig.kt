package com.buildsol.bottolshort.game.data

object DefaultLevelConfig {
    val value = LevelConfig(
        numColors = 4,
        bottleCapacity = 4,
        difficulty = Difficulty.MEDIUM,
        seed = null
    )
}
